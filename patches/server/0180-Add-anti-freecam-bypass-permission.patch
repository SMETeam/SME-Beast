From 0000000000000000000000000000000000000000 Mon Sep 17 00:00:00 2001
From: Snubby <87741849+SnubbyOWO@users.noreply.github.com>
Date: Sat, 20 Jun 2026 20:09:06 -0400
Subject: [PATCH] Add bypass permission lololol


diff --git a/src/main/java/com/destroystokyo/paper/antixray/ChunkPacketBlockControllerAntiXray.java b/src/main/java/com/destroystokyo/paper/antixray/ChunkPacketBlockControllerAntiXray.java
index 0efa04a16c1ebbbb36f041757d92177a75b4ce22..7fda8f20f3de16667b27ea12cbf0657a6d672f80 100644
--- a/src/main/java/com/destroystokyo/paper/antixray/ChunkPacketBlockControllerAntiXray.java
+++ b/src/main/java/com/destroystokyo/paper/antixray/ChunkPacketBlockControllerAntiXray.java
@@ -54,6 +54,7 @@ public class ChunkPacketBlockControllerAntiXray extends ChunkPacketBlockControll
             {0.5D, 0.5D, 0.125D},
             {0.5D, 0.5D, 0.875D}
     };
+    private static final String ANTI_FREECAM_BYPASS_PERMISSION = "smemc.antifreecam.bypass";
     private final ExecutorService executorService;
     private final boolean antiXrayEnabled;
     private final boolean asynchronous;
@@ -379,7 +380,7 @@ public class ChunkPacketBlockControllerAntiXray extends ChunkPacketBlockControll
     @Override
     public PacketPlayOutMapChunkInfoAntiXray getPacketPlayOutMapChunkInfo(PacketPlayOutMapChunk packetPlayOutMapChunk, Chunk chunk, int chunkSectionSelector, EntityPlayer player) {
         //Return a new instance to collect data and objects in the right state while creating the chunk packet for thread safe access later
-        PacketPlayOutMapChunkInfoAntiXray packetPlayOutMapChunkInfoAntiXray = new PacketPlayOutMapChunkInfoAntiXray(packetPlayOutMapChunk, chunk, chunkSectionSelector, player, this);
+        PacketPlayOutMapChunkInfoAntiXray packetPlayOutMapChunkInfoAntiXray = new PacketPlayOutMapChunkInfoAntiXray(packetPlayOutMapChunk, chunk, chunkSectionSelector, player, this, hasAntiFreecamBypass(player));
         packetPlayOutMapChunkInfoAntiXray.setNearbyChunks(chunk.world.getChunkIfLoaded(chunk.locX - 1, chunk.locZ), chunk.world.getChunkIfLoaded(chunk.locX + 1, chunk.locZ), chunk.world.getChunkIfLoaded(chunk.locX, chunk.locZ - 1), chunk.world.getChunkIfLoaded(chunk.locX, chunk.locZ + 1));
         return packetPlayOutMapChunkInfoAntiXray;
     }
@@ -391,7 +392,7 @@ public class ChunkPacketBlockControllerAntiXray extends ChunkPacketBlockControll
 
     @Override
     public boolean shouldTrackEntity(EntityPlayer player, Entity entity) {
-        if (!antiFreecamEnabled || !antiFreecamHideEntities || player == null || entity == null || player == entity || player.world != entity.world) {
+        if (!antiFreecamEnabled || !antiFreecamHideEntities || player == null || entity == null || player == entity || player.world != entity.world || hasAntiFreecamBypass(player)) {
             return true;
         }
 
@@ -571,7 +572,7 @@ public class ChunkPacketBlockControllerAntiXray extends ChunkPacketBlockControll
     }
 
     private void hideAntiFreecamBlocks(PacketPlayOutMapChunkInfoAntiXray packetPlayOutMapChunkInfoAntiXray) {
-        if (!antiFreecamEnabled || antiFreecamMaskMaxChunkSectionIndex < 0 || shouldRevealAntiFreecamChunk(packetPlayOutMapChunkInfoAntiXray.getPlayer(), packetPlayOutMapChunkInfoAntiXray.getChunk())) {
+        if (!antiFreecamEnabled || antiFreecamMaskMaxChunkSectionIndex < 0 || packetPlayOutMapChunkInfoAntiXray.hasAntiFreecamBypass() || shouldRevealAntiFreecamChunk(packetPlayOutMapChunkInfoAntiXray.getPlayer(), packetPlayOutMapChunkInfoAntiXray.getChunk())) {
             return;
         }
 
@@ -659,6 +660,10 @@ public class ChunkPacketBlockControllerAntiXray extends ChunkPacketBlockControll
             return true;
         }
 
+        if (hasAntiFreecamBypass(player)) {
+            return false;
+        }
+
         if (!isAntiFreecamUnderground(player)) {
             return true;
         }
@@ -675,6 +680,10 @@ public class ChunkPacketBlockControllerAntiXray extends ChunkPacketBlockControll
         return player != null && player.locY < (double) (antiFreecamMaskBelowY + antiFreecamVerticalGrace);
     }
 
+    private boolean hasAntiFreecamBypass(EntityPlayer player) {
+        return player != null && player.getBukkitEntity().hasPermission(ANTI_FREECAM_BYPASS_PERMISSION);
+    }
+
     private boolean isWithinAntiFreecamRadius(EntityPlayer player, int chunkX, int chunkZ, int radius) {
         int playerChunkX = MathHelper.floor(player.locX) >> 4;
         int playerChunkZ = MathHelper.floor(player.locZ) >> 4;
@@ -991,50 +1000,54 @@ public class ChunkPacketBlockControllerAntiXray extends ChunkPacketBlockControll
         boolean refreshTracker = false;
 
         synchronized (state) {
-            boolean underground = isAntiFreecamUnderground(player);
+            if (hasAntiFreecamBypass(player)) {
+                refreshTracker = state.clear();
+            } else {
+                boolean underground = isAntiFreecamUnderground(player);
 
-            if (!underground) {
-                if (state.underground) {
-                    refreshTracker = true;
-                }
+                if (!underground) {
+                    if (state.underground) {
+                        refreshTracker = true;
+                    }
+
+                    state.underground = false;
+                    state.pendingRevealChunks.clear();
+                    state.queuedRevealChunks.clear();
 
-                state.underground = false;
-                state.pendingRevealChunks.clear();
-                state.queuedRevealChunks.clear();
+                    if (state.surfaceTicks++ >= antiFreecamSurfaceRemaskDelayTicks) {
+                        queueAllAntiFreecamRemasks(state);
+                        state.centerChunkX = Integer.MIN_VALUE;
+                        state.centerChunkZ = Integer.MIN_VALUE;
+                        state.targetRadius = -1;
+                    }
+                } else {
+                    int chunkX = MathHelper.floor(player.locX) >> 4;
+                    int chunkZ = MathHelper.floor(player.locZ) >> 4;
+                    int targetRadius = getAntiFreecamTargetRadius(player);
+
+                    state.surfaceTicks = 0;
+
+                    if (!state.underground || state.centerChunkX != chunkX || state.centerChunkZ != chunkZ || state.targetRadius != targetRadius) {
+                        refreshTracker = true;
+                        state.underground = true;
+                        state.centerChunkX = chunkX;
+                        state.centerChunkZ = chunkZ;
+                        state.targetRadius = targetRadius;
+                        queueAntiFreecamReveals(state, chunkX, chunkZ, targetRadius);
+                    } else if (antiFreecamRepairIntervalTicks > 0 && player.ticksLived % antiFreecamRepairIntervalTicks == 0) {
+                        queueAntiFreecamReveals(state, chunkX, chunkZ, targetRadius);
+                    }
 
-                if (state.surfaceTicks++ >= antiFreecamSurfaceRemaskDelayTicks) {
-                    queueAllAntiFreecamRemasks(state);
-                    state.centerChunkX = Integer.MIN_VALUE;
-                    state.centerChunkZ = Integer.MIN_VALUE;
-                    state.targetRadius = -1;
+                    queueAntiFreecamRemasksOutside(player, state, chunkX, chunkZ, targetRadius);
                 }
-            } else {
-                int chunkX = MathHelper.floor(player.locX) >> 4;
-                int chunkZ = MathHelper.floor(player.locZ) >> 4;
-                int targetRadius = getAntiFreecamTargetRadius(player);
 
-                state.surfaceTicks = 0;
+                refreshTracker |= processAntiFreecamRemaskQueue(player, state);
+                refreshTracker |= processAntiFreecamRevealQueue(player, state);
 
-                if (!state.underground || state.centerChunkX != chunkX || state.centerChunkZ != chunkZ || state.targetRadius != targetRadius) {
+                if (state.entityTrackerRefreshNeeded) {
+                    state.entityTrackerRefreshNeeded = false;
                     refreshTracker = true;
-                    state.underground = true;
-                    state.centerChunkX = chunkX;
-                    state.centerChunkZ = chunkZ;
-                    state.targetRadius = targetRadius;
-                    queueAntiFreecamReveals(state, chunkX, chunkZ, targetRadius);
-                } else if (antiFreecamRepairIntervalTicks > 0 && player.ticksLived % antiFreecamRepairIntervalTicks == 0) {
-                    queueAntiFreecamReveals(state, chunkX, chunkZ, targetRadius);
                 }
-
-                queueAntiFreecamRemasksOutside(player, state, chunkX, chunkZ, targetRadius);
-            }
-
-            refreshTracker |= processAntiFreecamRemaskQueue(player, state);
-            refreshTracker |= processAntiFreecamRevealQueue(player, state);
-
-            if (state.entityTrackerRefreshNeeded) {
-                state.entityTrackerRefreshNeeded = false;
-                refreshTracker = true;
             }
         }
 
@@ -1814,6 +1827,24 @@ public class ChunkPacketBlockControllerAntiXray extends ChunkPacketBlockControll
         private final Deque<Long> pendingRevealChunks = new ArrayDeque<Long>();
         private final Set<Long> queuedRemaskChunks = new HashSet<Long>();
         private final Deque<Long> pendingRemaskChunks = new ArrayDeque<Long>();
+
+        private boolean clear() {
+            boolean changed = underground || entityTrackerRefreshNeeded || surfaceTicks != 0 || centerChunkX != Integer.MIN_VALUE || centerChunkZ != Integer.MIN_VALUE || targetRadius != -1
+                    || !revealedChunks.isEmpty() || !queuedRevealChunks.isEmpty() || !pendingRevealChunks.isEmpty() || !queuedRemaskChunks.isEmpty() || !pendingRemaskChunks.isEmpty();
+
+            underground = false;
+            entityTrackerRefreshNeeded = false;
+            surfaceTicks = 0;
+            centerChunkX = Integer.MIN_VALUE;
+            centerChunkZ = Integer.MIN_VALUE;
+            targetRadius = -1;
+            revealedChunks.clear();
+            queuedRevealChunks.clear();
+            pendingRevealChunks.clear();
+            queuedRemaskChunks.clear();
+            pendingRemaskChunks.clear();
+            return changed;
+        }
     }
 
     private static final class RayTraceChunk {
diff --git a/src/main/java/com/destroystokyo/paper/antixray/PacketPlayOutMapChunkInfoAntiXray.java b/src/main/java/com/destroystokyo/paper/antixray/PacketPlayOutMapChunkInfoAntiXray.java
index da65eaf738c896e7bc39d632e9e7d63247d6dea8..32e0ef6ac2a30f586201483d09bffcbec7d03921 100644
--- a/src/main/java/com/destroystokyo/paper/antixray/PacketPlayOutMapChunkInfoAntiXray.java
+++ b/src/main/java/com/destroystokyo/paper/antixray/PacketPlayOutMapChunkInfoAntiXray.java
@@ -13,14 +13,20 @@ public class PacketPlayOutMapChunkInfoAntiXray extends PacketPlayOutMapChunkInfo
     private Chunk[] nearbyChunks;
     private List<BlockPosition> rayTraceBlocks;
     private final ChunkPacketBlockControllerAntiXray chunkPacketBlockControllerAntiXray;
+    private final boolean antiFreecamBypass;
 
     public PacketPlayOutMapChunkInfoAntiXray(PacketPlayOutMapChunk packetPlayOutMapChunk, Chunk chunk, int chunkSectionSelector, ChunkPacketBlockControllerAntiXray chunkPacketBlockControllerAntiXray) {
-        this(packetPlayOutMapChunk, chunk, chunkSectionSelector, null, chunkPacketBlockControllerAntiXray);
+        this(packetPlayOutMapChunk, chunk, chunkSectionSelector, null, chunkPacketBlockControllerAntiXray, false);
     }
 
     public PacketPlayOutMapChunkInfoAntiXray(PacketPlayOutMapChunk packetPlayOutMapChunk, Chunk chunk, int chunkSectionSelector, @Nullable EntityPlayer player, ChunkPacketBlockControllerAntiXray chunkPacketBlockControllerAntiXray) {
+        this(packetPlayOutMapChunk, chunk, chunkSectionSelector, player, chunkPacketBlockControllerAntiXray, false);
+    }
+
+    public PacketPlayOutMapChunkInfoAntiXray(PacketPlayOutMapChunk packetPlayOutMapChunk, Chunk chunk, int chunkSectionSelector, @Nullable EntityPlayer player, ChunkPacketBlockControllerAntiXray chunkPacketBlockControllerAntiXray, boolean antiFreecamBypass) {
         super(packetPlayOutMapChunk, chunk, chunkSectionSelector, player);
         this.chunkPacketBlockControllerAntiXray = chunkPacketBlockControllerAntiXray;
+        this.antiFreecamBypass = antiFreecamBypass;
     }
 
     public Chunk[] getNearbyChunks() {
@@ -39,6 +45,10 @@ public class PacketPlayOutMapChunkInfoAntiXray extends PacketPlayOutMapChunkInfo
         this.rayTraceBlocks = rayTraceBlocks;
     }
 
+    public boolean hasAntiFreecamBypass() {
+        return antiFreecamBypass;
+    }
+
     @Override
     public void run() {
         chunkPacketBlockControllerAntiXray.obfuscate(this);
